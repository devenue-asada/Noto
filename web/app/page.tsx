import { Editor } from '@/app/components/Editor';
import { Room } from '@/app/components/Room';

const Page = () => {
    return (
        <Room>
            <Editor />
        </Room>
    );
};

export default Page;
